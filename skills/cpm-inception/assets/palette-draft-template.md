# Color Palette (Draft) — {project_name}

> **DRAFT — rapid-start foundation.** Refine with cpm-style-guide for full depth. The exact-hex requirement is NOT relaxed here: every allowed color is a `#RRGGBB` code, never a vague name — the Cinematographer rejects a name, and a vague color is a HOLD even in a draft.

## Allowed Colors

<!-- One color per row. Each MUST carry an exact hex code. -->

| Name | Hex | Usage Context |
|------|-----|---------------|
| {color name} | `{#RRGGBB}` | {what it is for and the theme it serves} |

## Forbidden Colors

| Color | Reason |
|-------|--------|
| {forbidden color, with a hex example} | {what it would wrongly signal in this film} |

## Color Meanings

<!-- What each allowed color signifies, so it is applied for meaning, not decoration. -->

- **`{#RRGGBB}` {color name}:** {the narrative meaning this color carries}
