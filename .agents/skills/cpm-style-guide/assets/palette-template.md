# Color Palette — {project_name}

> The exact-hex extract of the Cinematic Style Guide. Consumed by the Cinematographer during shard generation. Every allowed color is a hex code, never a vague name.

## Allowed Colors

<!-- One color per row. Each MUST carry an exact hex code. A vague name ("warm gold")
     is how a show drifts — the Cinematographer rejects it. -->

| Name | Hex | Usage Context |
|------|-----|---------------|
| {color name} | `{#RRGGBB}` | {what it is for and the theme it serves} |

## Forbidden Colors

| Color | Reason |
|-------|--------|
| {forbidden color, with a hex example} | {what it would wrongly signal in this film} |

## Color Meanings

<!-- What each allowed color signifies, so it is applied for meaning, not decoration. -->

- **`{#RRGGBB}` {color name}:** {the narrative meaning this color carries}
